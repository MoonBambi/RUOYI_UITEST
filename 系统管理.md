# 若依系统管理模块核心场景化测试用例

## 1. 系统管理

**系统管理**是若依后台的核心，负责整个系统的权限分配、组织架构定义及安全参数配置。场景化用例旨在模拟真实业务流，验证多个功能点之间的联动性。

---

## 2. 场景化用例详情

### 场景一：新员工入职（用户-部门-角色权限联动）

**描述**：模拟企业新员工入职时，管理员为其分配部门、设定角色并验证权限。

| **测试步骤** | **操作描述**                            | **预期结果**                        |
| -------- | ----------------------------------- | ------------------------------- |
| 1. 部门管理  | 创建一个新部门（如：测试部）                      | 部门树成功显示“测试部”，状态为正常              |
| 2. 角色管理  | 新增角色“软件测试员”，分配“系统管理-用户管理”菜单权限       | 角色创建成功，权限勾选已保存                  |
| 3. 用户管理  | 新增用户“test_user”，选择“测试部”，关联“软件测试员”角色 | 用户保存成功，所属部门和角色显示正确              |
| 4. 权限验证  | 使用 `test_user` 账号登录系统               | 侧边栏仅显示“系统工具“、”实例演示”，无法看到其他未授权模块 |

**测试重点**：数据的完整性、部门层级关系的展示、以及后端数据权限（Data Permission）的隔离。

---

### 场景二：安全合规控制（密码强度策略）

**描述**：验证系统对安全风险的防御能力，确保安全参数配置能实时生效。

| **测试步骤**  | **操作描述**                               | **预期结果**               |
| --------- | -------------------------------------- | ---------------------- |
| 1. 设置密码强度 | 在“参数设置”中修改 `sys.account.chrtype` 为 `1` | 修改成功                   |
| 2. 创建新用户  | 用户管理界面创建新用户，密码输入非纯数字                   | 创建用户失败，提示密码必须为纯数字（0-9） |
| 3. 创建新用户  | 用户管理界面创建新用户，密码输入纯数字                    | 创建用户成功                 |

**测试重点**：全局配置参数的实时生效性、Redis 缓存更新逻辑。

---

### 场景三：系统运维管理（在线用户监控与强制退登）

**描述**：模拟管理员处理异常登录或紧急清理在线会话。

| **测试步骤**  | **操作描述**                          | **预期结果**                    |
| --------- | --------------------------------- | --------------------------- |
| 1. 模拟多端登录 | 使用不同浏览器分别登录 `admin` 和 `test_user` | 两个账号均能正常操作                  |
| 2. 在线用户查看 | 进入“系统监控 -> 在线用户”模块                | 列表能实时显示当前的两个活跃 Session      |
| 3. 执行强退   | 管理员点击 `test_user` 对应的“强退”按钮       | 列表剔除该条记录，系统提示“强退成功”         |
| 4. 拦截验证   | 在 `test_user` 的浏览器中尝试刷新页面或点击菜单    | 页面自动跳转至登录页，提示“登录超时”或“会话已过期” |

**测试重点**：Session/Token 的失效机制、前后端交互的实时拦截。

---

### 场景四：审计追踪（操作日志闭环审计）

**描述**：验证系统是否能完整记录关键操作，用于事后回溯和定责。

| **测试步骤** | **操作描述**                      | **预期结果**                            |
| -------- | ----------------------------- | ----------------------------------- |
| 1. 触发操作  | 管理员在“岗位管理”中删除一个测试用的岗位记录       | 页面提示“删除成功”                          |
| 2. 日志检索  | 进入“系统监控 -> 操作日志”，根据“岗位管理”模块筛选 | 出现一条业务类型为“删除”的日志记录                  |
| 3. 详情审计  | 点击该日志查看“详情”                   | “请求参数”包含被删 ID，“操作状态”为成功，操作人显示 admin |
| 4. 异常模拟  | 尝试无权限或输入错误数据导致失败的操作           | 日志状态应记录为“异常”，并捕获错误信息                |

**测试重点**：日志记录的准确性（IP、地址、方法名）、敏感数据操作的留痕。
